// GitHub Copilot: Generate a Verilog wrapper for Caravel integration
// - Wrap the FSM overlay and PicoRV32 inside the Caravel framework
// - Connect FSM and CPU via a Wishbone bus or simple GPIOs
// - Include Caravel’s `user_project_wrapper` structure
